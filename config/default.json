{
    "comment": "deployment ENV: 'production' or 'development'",
    "NODE_ENV": "production",
    "AppPort": 4000,
    "_comment1": "LogLevel only affects production; error=0, warn=1, info=2, http=3, debug=4",
    "LogLevel": "info"
}